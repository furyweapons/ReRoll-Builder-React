import FooterButton from './FooterButton';

// Each folder has their own index file to work with internally
// Yes, it might look useless, but it's in case there were to be components added later
export {FooterButton};